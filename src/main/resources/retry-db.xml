<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!--引入数据库相关配置 !order指定其优先加载，ignore-unresolvable指定忽略不能匹配项-->
    <context:property-placeholder location="classpath:retry.conf" order="1" ignore-unresolvable="true"/>

    <bean id="retry_dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <property name="driverClass" value="${db.driverClass}" />
        <property name="jdbcUrl" value="${db.jdbcUrl}" />
        <property name="user" value="${db.user}" />
        <property name="password" value="${db.password}"/>

        <property name="minPoolSize" value="${c3p0.minPoolSize:5}" />
        <property name="maxPoolSize" value="${c3p0.maxPoolSize:30}" />
        <property name="initialPoolSize" value="${c3p0.initialPoolSize:10}" />
        <property name="maxIdleTime" value="${c3p0.maxIdleTime:60}" />
        <property name="idleConnectionTestPeriod" value="${c3p0.idleConnectionTestPeriod:60}" />
        <property name="acquireRetryAttempts" value="${c3p0.acquireRetryAttempts:30}" />
    </bean>

    <bean id="retry_sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="retry_dataSource" />
    </bean>

    <bean id="retry_transactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="retry_dataSource" />
    </bean>

    <bean id="retry_transactionTemplate"
          class="org.springframework.transaction.support.TransactionTemplate">
        <property name="transactionManager" ref="retry_transactionManager" />
    </bean>

</beans>