<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd">

    <bean class="com.retry.utils.SpringContextUtil" />

    <!--aop-->
    <context:component-scan base-package="com.retry.aspect" />
    <aop:aspectj-autoproxy />

    <bean class="com.retry.proxy.AutoProxy" />

    <import resource="retry-db.xml" />

    <!--retry task-->
    <bean id="retry_task" class="com.retry.scheduletask.RetryTask">
        <property name="clientDao" ref="clientDao"/>
        <property name="headersContext" ref="kepler.header.context" />
    </bean>

    <!--线程池-->
    <bean id="retry_executor" class="com.retry.scheduletask.RetryExecutor" />

    <!--zk监听器-->
    <bean id="retry_watcher" class="com.retry.zookeeper.RetryWatcher">
        <property name="taskHandler" ref="retry_handler" />
    </bean>
    <bean id="retry_root_watchar" class="com.retry.zookeeper.RootWatcher">
        <constructor-arg ref="retry_executor" />
        <property name="taskHandler" ref="retry_handler" />
    </bean>

    <!--zk session-->
    <bean id="retry_session" class="com.retry.zookeeper.Session" />

    <bean id="retry_handler" class="com.retry.scheduletask.TaskHandler">
        <constructor-arg ref="kepler.zookeeper" />
        <constructor-arg ref="retry_executor" />
        <constructor-arg ref="retry_session" />
        <constructor-arg ref="retry_task" />
        <property name="retryWatcher" ref="retry_watcher" />
        <property name="rootWatcher" ref="retry_root_watchar" />
    </bean>

</beans>